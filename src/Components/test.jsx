// {
//     <div className="transportation-cards-container">
//     {cards.map((c, i) => {
//       const oneStepValuesTransportation =
//         itinerary.steps[stepCount].transportation;
//       console.log(oneStepValuesTransportation);
//       const CurrentCard = availableCards.transportation;

//       return oneStepValuesTransportation.map((resourceId, k) => (
//         <CurrentCard key={k + 100} resourceId={resourceId} />
//       ));
//     })}
//     </div>
//     <div className="accomodation-cards-container">
//     {cards.map((c, i) => {
//       const oneStepValuesAccomodation =
//         itinerary.steps[stepCount].accomodation;
//       const CurrentCard = availableCards.accomodation;

//       return oneStepValuesAccomodation.map((resourceId, k) => (
//         <CurrentCard key={k + 100} resourceId={resourceId} />
//       ));
//     })}
//     </div>
//     <div className="activity-cards-container">
//     {cards.map((c, i) => {
//       const oneStepValuesActivity = itinerary.steps[stepCount].activity;
//       const CurrentCard = availableCards.activity;

//       return oneStepValuesActivity.map((resourceId, k) => (
//         <CurrentCard key={k + 100} resourceId={resourceId} />
//       ));
//     })}
//     </div>
//     }
